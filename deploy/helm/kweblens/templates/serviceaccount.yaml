{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kweblens.serviceAccountName" . }}
  labels:
    {{- include "kweblens.labels" . | nindent 4 }}
{{- end -}}
